<%== erb :'fixtures/filter_form', locals: { criteria: } %>
<% unless matches.empty? %>
  <br />
  <% matches.map { |match| match[:match_date] }.uniq.sort.each do |day| %>
    <% day_matches = matches.filter { |match| match[:match_date] == day } %>
    <%== erb :'fixtures/day',
             locals: { day:, matches: day_matches, match_ids: } %>
  <% end %>
<% end %>
<script type="text/javascript" src="/js/fixtures.js"></script>
