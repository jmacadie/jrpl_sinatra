<nav class="navbar navbar-expand-md navbar-dark bg-dark fixed-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">JRPL</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#jrplNavbar"
      aria-controls="jrplNavbar"
      aria-expanded="false"
      aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="jrplNavbar">
      <ul class="navbar-nav ms-auto mb-2 mb-md-0">
        <% if session[:user_id] %>
        <li class="nav-item dropdown">
          <a
            class="nav-link dropdown-toggle"
            href="#"
            role="button"
            data-bs-toggle="dropdown"
            aria-expanded="false">
            Signed in as <strong><%== session[:user_name] %></strong>
          </a>
          <ul class="dropdown-menu dropdown-menu-end">
            <li>
              <form class="signout" method="post" action="/users/signout">
                <%== erb :'forms/authenticity_token' %>
                <button type="submit" class="dropdown-item">Sign Out</button>
              </form>
            </li>
            <li><a class="dropdown-item" href="/users/edit_credentials">Edit Details</a></li>
          </ul>
        </li>
        <li class="nav-item"><a class="nav-link<%== active_if(page, 'fixtures') %>" href="/fixtures">Fixtures</a></li>
        <li class="nav-item"><a class="nav-link<%== active_if(page, 'tables') %>" href="/tables">Tables</a></li>
        <li class="nav-item"><a class="nav-link<%== active_if(page, 'graphs') %>" href="/graphs">Graphs</a></li>
        <li class="nav-item"><a class="nav-link<%== active_if(page, 'rules') %>" href="/rules">Rules</a></li>
        <% else %>
        <li class="nav-item"><a class="nav-link" href="/users/signin">Sign In</a></li>
        <li class="nav-item"><a class="nav-link" href="/users/signup">Create Account</a></li>
        <% end %>
        <% if user_is_admin? %>
        <li class="nav-item"><a class="nav-link" href="/admin">Admin</a></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
