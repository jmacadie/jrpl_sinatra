<h3>
  <%== match[:stage] %><br />
  <%== date_for_print(match) %><br />
  <%== kick_off_for_print(match) %><br />
  <small>
    <%== "#{match[:venue]}, #{match[:city]}" %><br />
    <%== erb :'match/details_broadcaster',
             locals: { match:, broadcasters: } %>
  </small>
</h3>
<%== erb :'match/details_big', locals: { match:, origin: } %>
<%== erb :'match/details_small', locals: { match:, origin: } %>
