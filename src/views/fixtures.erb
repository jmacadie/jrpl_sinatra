<%== erb :'fixtures/filter_form' %>
<% unless @matches.empty? %>
  <% @matches.map { |m| m[:match_date] }.uniq.sort.each do |d| %>
    <% @day_matches = @matches.filter { |m| m[:match_date] == d } %>
    <% @day = d %>
    <%== erb :'fixtures/day' %>
  <% end %>
<% end %>
<script type="text/javascript" src="/js/fixtures.js"></script>